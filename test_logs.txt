possible culprits for why there are too many danger tiles:

1. Maybe adding the ability to unvalidate a box could allow the inner map to self regulate false flags...
2. Defaulting to the previous depth if the gap's depth is not valid might lead to sus behaviour:
else:
    # No valid depth in the gap – judge it at the nearer
    # obstacle's distance (conservative → merge).
    d_gap = min(cur[2], nxt[2])

3. cur and nxt are mutable, this could completely mess with the blocks logic:
if gap_m >= self._passable_m:
    merged.append(cur)
    cur = nxt
    continue